# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'Ghost Management' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Ghost Management
  pod 'Google-Mobile-Ads-SDK'
end

target 'pod \'Google-Mobile-Ads-SDK\'' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for pod 'Google-Mobile-Ads-SDK'

  target 'pod \'Google-Mobile-Ads-SDK\'Tests' do
    # Pods for testing
  end

end
